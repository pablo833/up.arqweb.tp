export default class Task {
    constructor(name, board, column, description) {
        this.name = name;
        this.board = board;
        this.column = column;
        this.description = description;
    }
}